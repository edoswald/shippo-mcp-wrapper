{
  "name": "shippo-mcp-wrapper",
  "version": "1.0.0",
  "description": "Railway wrapper exposing Shippo shipping tools as a header-authenticated SSE MCP endpoint for Retell AI (Hal)",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "vitest --run"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fast-check": "^3.0.0"
  }
}
